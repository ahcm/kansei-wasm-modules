[package]
name = "meilisearch"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
ureq = { version = "2.10", default-features = false }

[profile.release]
opt-level = "z"
